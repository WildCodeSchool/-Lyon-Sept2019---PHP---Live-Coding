{% extends 'base.html.twig' %}

{% block title %}Hello RentalController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Rentals</h1>

    <ul>
    	{% for house in houses %}
    		<li><a href="{{ path('rental_view', {id: house.id}) }}">{{ house.title }} - {{ house.city.name }}</a></li>
    	{% endfor %}
    </ul>
   {# <h1>By city ...</h1>
    <ul>
    	{% for city in cities %}
    		<li>{{city.name}}: <br/>
    			<ul>
    				{% for house in city.houses %}
    					<li>{{ house.title }}</li>
    				{% else %}
	    				<li><em>Y a rien ici !</em></li>
    				{% endfor %}
    			</ul>
    		</li>
    	{% endfor %}
    </ul>#}
</div>
{% endblock %}
